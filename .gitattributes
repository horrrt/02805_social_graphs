# The build stamp in these two pages is generated by scripts/stamp_week03.py.
# A merge should never stop to ask a human which hash to keep; the driver
# blanks it, merges the real content, and the stamp script writes it back.
# Install with: python3 scripts/stamp_week03.py --hook
docs/weeks/week03/index.html merge=stamped
docs/styleguide/index.html    merge=stamped
